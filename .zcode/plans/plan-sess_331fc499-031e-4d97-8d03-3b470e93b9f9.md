# Remaining Implementation Plan (continuation of approved audit + security roadmap)

Already completed this session: Phase 0 security fixes (migration 040, role-switching UI removal, credential scrub, strict validation), WAITING_* state transitions, and PERF-003 PATCH state-machine enforcement. All 180 tests pass.

## Phase 1 — Foundation (security plan)
1. **1.1 SQL customer counts** — replace O(all-tickets) count maps in `server/repository.ts` (`listCustomers`, `countTicketsBy*`) with SQL aggregates / `head:true, count:'exact'`
2. **1.2 SLA timezone correctness** — rewrite business-hours math in `src/lib/slaCalculator.ts` to honor `tzName` via `Intl` wall-clock conversion (currently uses server-local `getHours()`)
3. **1.3 RBAC tier differentiation** — `server/rbac.ts` + `src/lib/rbac.ts`: L1 (no delete/merge/escalate/admin), L2 (+resolve, escalate, unmask, merge), L3 (+delete, admin:config, audit:write); legacy BU_SUPPORT keeps full set
4. **1.4 Partner org FK wiring** — migration 041: `users.partner_org_id` + backfill from partner name; populate in `toAuthUser`/`listUsersPublic`; prefer FK equality over `ilike` string matching in `repository.ts` scoping
5. **1.5 Atomic config replace** — migration: `replace_table_rows(p_table, p_rows)` SECURITY DEFINER RPC (whitelisted tables); `repository.replaceJsonTable` calls it
6. **1.6 `requireAuth` async fix** — `server/auth.ts`: convert Promise.then chain to async middleware
7. **1.7 DB transition trigger** — migration: trigger validating ticket status changes against the transition adjacency list
8. **1.8 SLA dedupe persistence** — migration: `sla_notification_log` with unique key; `server/slaJob.ts` uses insert-with-ignoreDuplicates instead of in-memory Map

## Phase 2 — Product improvements
1. SLA pause/resume: `sla_paused_ms`/`sla_pause_started_at` columns + state-machine mutate hooks + effective-deadline helper; SLA job skips WAITING_*
2. Implement `usePermissions`/`useHasPermission` stubs properly (`src/hooks/queries.ts`)
3. Partner reply transition already added (WAITING_PARTNER → INVESTIGATE for partners)

## Phase 3 — Performance quick wins
1. `express.static` immutable caching for `/assets` + `no-cache` for `index.html` (`server.ts`)
2. Bootstrap reads in parallel (`Promise.all` in `server/routes/operations.ts`)
3. Dynamic-import jspdf in `src/lib/pdfGenerator.ts`; dynamic xlsx in `bulkImport.ts`/`parseConfigUpload.ts`
4. Self-host fonts decision: add `preconnect` + `font-display: swap` (keep Google Fonts, fix render-blocking `@import` → `<link>`)
5. `index.html` meta description/OG; `robots.txt` + `sitemap.xml` (public routes only)
6. Evidence thumbnails `loading="lazy"`; fix `queryKeys.comments` to key by ticketId

## Phase 4 — Rendering/reliability
1. SSE auto-reconnect with backoff (`src/lib/api.ts connectEvents`)
2. Move `saveToStorage` call out of the `setWatcherNotifications` updater (StrictMode double-invoke)
3. `listTickets` envelope: query-level scoping (tenant + partner FK), remove in-JS `canAccessTicket` post-filter, single `{items,total}` shape — update the 3 route tests that assert current shapes

## Phase 5 — State architecture (merged Sec 2.5/2.6 + Perf Phase 4)
1. Remove localStorage (`4c_*`) + Dexie domain mirrors: context becomes in-memory only, hydrated from bootstrap; delete `saveToStorage` call sites
2. Remove Dexie offline-mutation replay from `main.tsx`; `clearAppData` becomes key cleanup
3. Light URL routing: sync `activeTab`/`activeTicketId` to `history.pushState` + `popstate` restore (deep links + back button) without remounting the SPA

## Phase 6 — Tests & validation
1. New tests: RBAC tier permissions, SLA timezone cases, partner-org isolation, WAITING transitions (extend `ticketStateMachine.test.ts`)
2. Run `vitest`, `tsc --noEmit`, `eslint`, `vite build`; record bundle sizes before/after

## Phase 7 — Documentation rewrite (all stub .md files)
Rewrite `docs/00-24` with substantive content derived from the final implementation: PRD, FRD, SRS, architecture, database design, RBAC matrix, workflows, API spec, UI/UX guidelines, notification framework, SLA & escalation spec, compliance/security, reporting, testing strategy, deployment guide, roadmap/backlog. Update `25_GoLive` and `DESIGN_SYSTEM.md` (font contradiction) to match reality.

## Execution rules
- Each phase fully implemented and verified (tests green) before moving on
- No functionality removed without replacement; frontend callers migrated alongside API changes
- Final report: changes made, before/after bundle + payload measurements, validation results, remaining issues