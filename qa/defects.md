# Defect Register

| ID | Severity | Module | Symptom | Root Cause | Business Impact | Security Impact | Required Fix | Status |
|---|---|---|---|---|---|---|---|---|
| SEC-18 | CRITICAL | server/routes/tickets.ts:98,113 | `GET /api/tickets?unmask=true` (and `/tickets/:id`) returned full card PAN for any authenticated user, incl. PARTNER role that lacks `tickets:unmask` | Route read `req.query.unmask === 'true'` and forced unmasked output with only `requirePermission('tickets:view')`; the `tickets:unmask` permission defined in `server/rbac.ts` was never checked in the tickets routes | Card PAN + PII leakage to payment-partner accounts that should only ever see masked data | Raw PAN and PII (email/phone) exposed over the API on a query-string toggle | **FIXED & VERIFIED**: route now resolves roles via `getCachedRoles` and returns 403 for `?unmask=true` when the caller lacks `tickets:unmask` (list + detail). Live-confirmed 2026-08-15: partner → 403 on both; default view still masked. Regression tests `tests/unmask-access.test.ts` (5 cases). Full suite 307/307, tsc + lint clean. | FIXED (approved 2026-08-15) |
| SEC-56 | LOW | server/routes/tickets.ts createTicketSchema | `POST /api/tickets` with empty body returns 500 `"A customer email and business unit are required"` instead of 400 | `createTicketSchema` (zod) makes `customerEmail`/`businessUnit` optional; the real requirement-sanity check lives in `findOrCreateCustomer` (repository.ts:693) which throws → uncaught 500 | Client sees server error instead of validation message; QoL/API-contract issue only | None (no data exposed; ticket correctly not created) | Tighten createTicketSchema: require customerEmail + businessUnit (trimmed non-empty) so invalid input returns 400 via validateBody; optionally wrap findOrCreateCustomer throw in 400 | FIXED + verified live (2026-08-15): empty body → 400 `customerEmail: Customer email is required`; no-email → 400; regression tests added (unmask-access.test.ts:132-163); suite 310/310 |
| (add more rows here) | | | | | | | | |

Severity: CRITICAL / HIGH / MEDIUM / LOW